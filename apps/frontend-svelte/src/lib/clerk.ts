// Clerk is now managed by svelte-clerk's ClerkProvider.
// Use components from 'svelte-clerk' directly (SignIn, SignedIn, SignedOut, UserButton, etc.)
// Use useClerkContext() inside components to access auth state.
export {};
